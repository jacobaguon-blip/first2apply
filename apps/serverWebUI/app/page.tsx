// Placeholder home page for the server web UI scaffold.
// Real Next.js wiring (next.config.js, tailwind.config.ts, shadcn install) is a Monday item.

export default function Home() {
  return (
    <main>
      <h1>first2apply server</h1>
      <p>scaffold — see deploy/pi/ for deploy assets.</p>
    </main>
  );
}
