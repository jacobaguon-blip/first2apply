/// <reference types="next" />
/// <reference types="next/image-types/global" />

// Replaces next-env.d.ts which Next.js auto-generates at dev/build.
// Committed so `nx typecheck` works on a fresh clone without first running `next build`.
