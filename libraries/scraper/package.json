{
  "name": "@first2apply/scraper",
  "version": "0.0.0",
  "type": "commonjs",
  "main": "build/index.js",
  "typings": "build/index.d.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@first2apply/core": "workspace:*",
    "@supabase/supabase-js": "^2.39.0",
    "exponential-backoff": "^3.1.1",
    "luxon": "^3.4.4",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/luxon": "^3.4.2",
    "@types/node": "^22.0.0",
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.6.3"
  }
}
